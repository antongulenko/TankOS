
== Sanity tests

- AVR with simple LED blinking routine. Try to measure _delay_ms precision.
- Flash full Tank-IO kernel, test startup routine with blinking.
- Add complete button/LED circuit, test.
- Add TWI curcuit with Beaglebone.

== Milestones

- Use Processes, Scheduler?
- Makefiles
		- Add avrdude commands: flash (inkl. chip-erase), (read+write eeeprom)

== Refactorings

- Extract HAL layer in TankOS, with generic interface and multiple implementations
    - implementations based on platform: AVR, Linux, Simulation
    - folders in TankOS, only
- Interrupt handler mit Processes/scheduling integrieren?

== TODO

- find out, why Release build does not work properly. At least the _delay_ms function produces wrong results. Must be something about the optimizations.

- PWM (8-bit timer) mit maximalem wert eingestellt (256) liefert nur 4,6V !!!
	-> entweder fixen oder dann einen elektronischen workaround

- alle Buttons resetten (?) Reset-Leitung auf den Kippschalter legen.
