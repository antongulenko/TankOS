- find out, why Release build does not work properly. At least the _delay_ms function produces wrong results. Must be something about the optimizations.

== Stand der Dinge
. Normales Ansteuern der Motoren funktoniert.
. Smooth Motors: der algorithmus scheint zu gehen, im Simulator funktioniert auch alles.
	(zumindest mit der Loop-variante, die Interrupt-variante wurde noch nicht getestet)
	Im Roboter bleibt die PWM spannung aber tatsächlich auf einem sehr niedrigen (bzw hohen) level. Dieser Level wird aber scheinbar 'smooth' erreicht. Es ist unklar, warum an der stelle schluss ist und die geschwindigkeit nicht weiter geht.

. Der Compare Output Mode war im Simulator einmal unterschiedlich für Timer1A und Timer1B. setCompareMatchOutputMode() prüfen.
